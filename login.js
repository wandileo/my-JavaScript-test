let names = [];
let password = [];

function addLogin() {
    var names = document.getElementById("names");

    if (names == "") {
        alert("Email or Password is incorrect");
        return;
    }
}